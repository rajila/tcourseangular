import { FiltrarCaracteresDirective } from './filtrar-caracteres.directive';

describe('FiltrarCaracteresDirective', () => {
  it('should create an instance', () => {
    const directive = new FiltrarCaracteresDirective();
    expect(directive).toBeTruthy();
  });
});
