import { PipeimpuppercasePipe } from './pipeimpuppercase.pipe';

describe('PipeimpuppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeimpuppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
