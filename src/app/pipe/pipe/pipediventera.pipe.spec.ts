import { PipediventeraPipe } from './pipediventera.pipe';

describe('PipediventeraPipe', () => {
  it('create an instance', () => {
    const pipe = new PipediventeraPipe();
    expect(pipe).toBeTruthy();
  });
});
