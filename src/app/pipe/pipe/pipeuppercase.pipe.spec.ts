import { PipeuppercasePipe } from './pipeuppercase.pipe';

describe('PipeuppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new PipeuppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
